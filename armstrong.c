#include <stdio.h>
#include <math.h>
int main() {
    int count=0,num,sum=0,rem;
    printf("enter any number");
    num=n;
    while(num!=0)
    {
        rem=num/10;
        count++;
    }
    while(num!=0)
    
    return 0;
}
